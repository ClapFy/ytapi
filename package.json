{
  "name": "ytapi",
  "private": true,
  "scripts": {
    "build": "turbo run build",
    "dev": "turbo run dev",
    "lint": "turbo run lint",
    "type-check": "turbo run type-check",
    "start": "cd apps/api && npm start"
  },
  "devDependencies": {
    "turbo": "^1.11.0"
  },
  "workspaces": [
    "apps/*",
    "packages/*"
  ]
}